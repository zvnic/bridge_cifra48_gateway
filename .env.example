# Порт приложения (для docker-compose — маппинг хоста)
PORT=8000

# Бэкенд bridge (обязательно)
BRIDGE_BASE_URL=https://bridge-back.admlr.lipetsk.ru
BRIDGE_COMPLETIONS_URL=https://bridge-back.admlr.lipetsk.ru/api/v1/completions

# Имя модели, которое видит n8n
MODEL_NAME=deepseek-v3

# Модель, передаваемая на bridge (если у бэкенда другое имя)
BRIDGE_MODEL=deepseek-ai/DeepSeek-V3-0324

# Опционально: ключ по умолчанию (если клиент не передаёт Authorization / X-API-Key)
# DEFAULT_API_KEY=your_api_key_here
